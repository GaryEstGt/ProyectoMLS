@model Lab1MLS.Models.Jugador

@{
    ViewBag.Title = "EliminarPorArchivo";
}

<h2>EliminarPorArchivo</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Jugador</h4>
    <hr />
       @using (Html.BeginForm("EliminarPorArchivo", "Jugador", FormMethod.Post, new { enctype = "multipart/form-data" }))
       {
        <input type="file" name="postedFile" />
        <input type="submit" value="Eliminar" />
       }

    

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

 
    }
</div>
